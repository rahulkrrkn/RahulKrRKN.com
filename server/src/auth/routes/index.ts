import express, { Router } from "express";

const app: Router = express.Router();

// === === === === ===
// Imports
// === === === === ===

// import login from "./login.route.js";

// === === === === ===
// Routes
// === === === === ===

// app.use("/login", login);

export default app;
